# Release policy

Use conventional commits and pull requests. CI runs formatting, lint, typecheck, unit tests, design checks, production build, Storybook and browser tests.

Every release includes:

- user-visible summary
- contract/config/migration changes
- test evidence
- telemetry and alert updates
- known limitations
- rollout and rollback
- owner and approval

Do not label environment-blocked checks as passed.
